[package]
name = "jelly-lib"
version = "0.1.1"
edition = "2021"

[dependencies]
nix = { version ="0.30.1", features = ["fs", "ioctl"], optional = true }
libc = { version = "0.2.155", optional = true }
libm = "0.2"

[features]
default = ["std"]
std = ["nix", "libc"]
